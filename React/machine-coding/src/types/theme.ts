export type Theme = {
  colors: {
    primary: string;
    secondary: string;
    background: string;
    text: string;
    header: string;
    navbar: string;
    button: string;
    buttonText: string;
  };
  fonts: {
    primary: string;
  };
  spacing: {
    padding: string;
  };
};
