export enum RouteId {
  Home,
  Table,
  TicTacToe,
  Stepper,
  Switch,
  NotFound,
}

export enum Routepath {
  Home = "/",
  ticTacToe = "/tic-tac-toe",
  table = "/table",
  stepper = "/stepper",
  switch = "/switch",
  NotFound = "*",
}
