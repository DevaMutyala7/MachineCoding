export enum RouteId {
  Home,
  Table,
  TicTacToe,
  Stepper,
  NotFound,
}

export enum Routepath {
  Home = "/",
  ticTacToe = "/tic-tac-toe",
  table = "/table",
  stepper = "/stepper",
  NotFound = "*",
}
